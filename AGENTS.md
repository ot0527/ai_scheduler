# AI秘書スケジュール — エージェント向けガイド

## コーディング原則

### ドキュメンテーション

- 関数・メソッド・クラス・公開型には、各言語の標準 doc コメントを**日本語**で記述する
  - TypeScript: JSDoc（`@param`, `@returns`, `@example`）
- 「何をするか」「なぜそうするか」を書く。「どうやるか」はコードで表現する

### アーキテクチャ

- 保守性・拡張性・可読性を重視する
- 責務を明確に分離する（単一責任）
- 密結合を避け、依存関係の方向を意識する
- 将来の変更に対して開かれた設計にする（拡張に対して開、修正に対して閉）
- 過度な抽象化は避け、現時点で必要な分だけ設計する

### 実装前の調査

- ライブラリ利用時は最新の公式ドキュメントを確認する
- 非推奨 API や古い書き方を避ける

## プロジェクト構成

```text
apps/web/           … UI・Supabase クライアント（プレゼンテーション層）
packages/core/      … ドメインロジック・Zod スキーマ・DB 型（フレームワーク非依存）
supabase/           … マイグレーション・RLS
docs/               … 企画書・技術選定
```

### レイヤー分担

| 処理 | 置き場所 |
| --- | --- |
| 空き時間計算・バリデーション | `packages/core` |
| DB 行 → ドメイン型の変換 | `packages/core/src/mappers` |
| Supabase CRUD・React Query | `apps/web/src/hooks`, `apps/web/src/lib` |
| 画面・フォーム | `apps/web/src/pages`, `apps/web/src/components` |

### 拡張時の指針

- Phase 追加時は `packages/core` にロジックを先に置き、UI は薄く保つ
- 新テーブル追加時は `database-types.ts` と mapper を同時に更新する
- 定数・ラベルは一箇所に集約し、UI とロジックで重複定義しない
- Query Key は `apps/web/src/lib/query-keys.ts` に集約する

## セキュリティ（必須）

- 全 public テーブルで RLS を有効化する
- API キーはクライアントに渡さない（Edge Function 経由）
- AI 出力は Zod で検証し、ユーザー承認前に DB へ反映しない

## コミュニケーション

- 日本語で応答する
- 不明点があれば実装前に質問する
- 大きな変更の前にはプランを提示して確認を取る

## 参照ドキュメント

- [詳細企画書](./docs/AI秘書スケジュール管理アプリ_詳細企画書_セキュリティ反映版.md)
- [技術選定と実装方針](./docs/技術選定と実装方針.md)
