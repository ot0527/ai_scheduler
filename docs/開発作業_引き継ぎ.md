# 開発作業 引き継ぎメモ

> 新しいチャットへ引き継ぐための作業サマリー  
> 最終更新: 2026年5月24日（Phase 5 完了・AI 相談正規化修正反映）

---

## 1. このリポジトリの目的

**AI秘書スケジュール管理アプリ** — 長期目標・生活リズム・固定予定をもとに日々の行動計画を作成・調整する Web アプリ。

- 完成版仕様を目指す（MVP ではない）
- 参照企画書: [`AI秘書スケジュール管理アプリ_詳細企画書_セキュリティ反映版.md`](./AI秘書スケジュール管理アプリ_詳細企画書_セキュリティ反映版.md)
- 技術方針: [`技術選定と実装方針.md`](./技術選定と実装方針.md)

---

## 2. 現在の進捗

| Phase                                      | 状態      | 内容                                       |
| ------------------------------------------ | --------- | ------------------------------------------ |
| **企画・設計**                             | ✅ 完了   | 詳細企画書、技術選定ドキュメント           |
| **Phase 0（基盤）**                        | ✅ 完了   | モノレポ、Supabase、Auth、RLS、CI          |
| **Phase 1（生活リズム・固定予定）**        | ✅ 完了   | CRUD、当日変更、空き時間計算、Notion 風 UI |
| **Phase 2（目標 + AI 分解）**              | ✅ 完了   | 目標 CRUD、BYOK、Edge Functions、承認 UI   |
| **Phase 3（時間予算 + 日次スケジュール）** | ✅ 完了   | 週次予算、スコアリング配置、承認 UI        |
| **Phase 4（実行・リスケ）**                | ✅ 完了   | 完了記録、振り返り、小/大規模リスケ        |
| **Phase 5（AI 相談・設定・データ権）**     | ✅ 完了   | AI 相談、データ権、通知、月間トークン上限  |

**Phase 0〜5 は一通り実装済み。残タスクは §18（任意拡張）を参照。**

---

## 3. これまでの作業の流れ

### 初回チャット（2026-05-22）

1. 企画書をもとに [`技術選定と実装方針.md`](./技術選定と実装方針.md) を作成
2. Phase 0 + Phase 1 を実装（Notion 風 UI）
3. [`README.md`](../README.md) に環境構築手順を記載
4. ユーザー指摘を受けて [`AGENTS.md`](../AGENTS.md) を整備し、コードを保守性重視でリファクタ

### フォローアップ（2026-05-23）

5. オンボーディング遷移バグ修正（React Query キャッシュ）
6. 休日就寝デフォルト `23:00` 化
7. [`テーブル設計_Phase1.md`](./テーブル設計_Phase1.md) 追加
8. `favicon.svg`、`.gitignore` 整備

### Phase 2 実装（2026-05-23〜24）

9. **DB** — `goals`, `goal_components`, `work_block_templates`, `user_ai_settings`, `ai_request_logs` + RLS + Vault RPC
10. **Edge Functions** — `goal-decompose`, `goal-approve-decompose`, `ai-settings`（OpenAI / Gemini 対応、BYOK）
11. **`packages/core`** — 目標 Zod スキーマ、AI 出力検証、正規化レイヤー（`normalizeGoalDecomposeOutput`）
12. **UI** — 目標一覧/登録/詳細、AI 分解確認・承認、AI 設定（BYOK）
13. **運用トラブル対応**
    - `supabase link` 未実行 → `db push` 失敗
    - Phase 1 済み DB → `migration repair 20260522000000 --status applied`
    - Edge Function デプロイ → `packages/core/dist` 参照 + 各関数 `deno.json`（`zod` npm 解決）
    - Vault キー取得 → `get_api_key_by_ref` RPC 追加
    - Gemini 出力スキーマ不一致 → 正規化 + プロンプト改善（`late_night` → `night` 等）
    - エラー表示 → `FunctionsHttpError` から本文を抽出

### Phase 3 実装（2026-05-24）

14. **DB** — `goal_budgets`, `schedules`, `scheduled_blocks`, `alerts` + RLS
15. **Edge Functions** — `budget-calculate`, `schedule-generate`（非 AI、JWT 必須）
16. **`packages/core`** — 週次予算（`budget.ts`）、スコアリング（`scoring.ts`）、貪欲配置（`placement.ts`）、検証（`validation.ts`）、週次空き時間（`weekly-free-time.ts`）
17. **UI** — `/budget`（時間予算）、`/schedule/approve`（仮スケジュール承認）、ホームに作業予定・アラート表示
18. **テスト** — `budget-placement.test.ts` 追加（計 17 tests）

### Phase 3 デプロイ・AI トラブル対応（2026-05-24〜25）

19. **Edge Function デプロイ失敗（`date-fns`）** — `week-utils.ts` から `date-fns` を除去し、ネイティブ `Date` API に変更（Deno バンドルは dist 内の npm 依存を解決できない）
20. **Edge Function デプロイ失敗（`zod`）** — `schedule-utils.ts` が `dist/index.js` を import していたため zod 依存が連鎖。Phase 3 関数は **`dist/scheduling/*.js` 等を直接 import** するよう修正
21. **Gemini JSON 途切れ** — 大きな目標（例: LoL グランドマスター）で `summary` 途中で JSON が切れる事象
    - `call-ai.ts`: `maxOutputTokens: 8192`、`finishReason === MAX_TOKENS` 検知
    - JSON パース失敗・途切れ時に **1 回自動リトライ**（短縮ヒント付き）
    - プロンプト: `summary` 300 文字以内、components 最大 12、workBlocks 最大 24

### Phase 4 実装（2026-05-24）

22. **DB** — `schedules` に `fatigue_level` / `review_note` / `reviewed_at` 追加、`scheduled_blocks.status` インデックス
23. **Edge Functions** — `schedule-reschedule-minor`（非 AI）、`reschedule-major`（AI、BYOK 必須）
24. **`packages/core`** — `execution.ts`（完了記録）、`reschedule-minor.ts`（小規模リスケ）、`ai/prompts|schemas/reschedule.ts`（大規模リスケ）、`scoring.ts` に疲労度・過去選択連動
25. **UI** — `/review`（完了記録・振り返り・リスケ）、ホームにステータス表示、承認画面にキャンセル・`?date=` 対応
26. **テスト** — `execution-reschedule.test.ts` 追加（計 26 tests）

### Phase 5 実装（2026-05-24）

27. **DB** — `user_preferences.notification_settings`（JSONB）、`user_ai_settings.ai_tone`、`purge_old_ai_request_logs` RPC（90日超ログ削除）
28. **Edge Functions** — `ai-chat`（AI 相談）、`export-data`（JSON エクスポート）、`delete-account`（Vault キー破棄 + auth ユーザー削除）
29. **`packages/core`** — `ai/prompts|schemas/chat.ts`、`normalize-chat.ts`（`suggestedActions` の未知キー除去）、`schemas/notification-settings.ts`（アラート伏せ字）
30. **`ai-utils.ts` 拡張** — 月次トークン加算・リセット・上限チェック（`assertAIUsageAllowed`）、AI 相談日次上限（20 回/日）
31. **UI** — `/chat`（AI 相談）、`/settings/notifications`（通知プライバシー）、`/settings/data`（エクスポート/削除）、振り返り履歴、AI 口調設定
32. **テスト** — `phase5.test.ts` 追加（計 32 tests）

### Phase 5 トラブル対応（2026-05-24）

33. **AI 相談 502（Zod 検証失敗）** — Gemini が `suggestedActions` に `payload` 等の余分キーを返す
    - `normalizeChatOutput` で `label` / `description` のみ残して正規化
    - プロンプトに「追加キーを付けない」と明記
    - **`ai-chat` 再 deploy 必須**（`core build` 後）

---

## 4. 技術スタック

| レイヤー     | 選定                                          |
| ------------ | --------------------------------------------- |
| モノレポ     | pnpm workspaces + Turborepo                   |
| フロント     | React 19, Vite, TypeScript, Tailwind CSS v4   |
| 状態管理     | TanStack Query v5                             |
| BaaS         | Supabase（Auth, PostgreSQL, RLS, Vault）      |
| サーバー     | Supabase Edge Functions（Deno）               |
| 共有ロジック | `@ai-scheduler/core`（Zod, Vitest）           |
| CI           | GitHub Actions（型チェック + ユニットテスト） |

---

## 5. ディレクトリ構成

```text
ai_scheduler/
├── AGENTS.md
├── README.md
├── apps/web/
│   └── src/
│       ├── pages/            # 画面（目標・予算・承認・振り返り含む）
│       ├── components/
│       ├── hooks/            # useScheduleData, useGoals, useBudgets, useSchedules, useExecution, useChat, usePhase5, useDataRights
│       └── lib/              # supabase, query-keys, edge-functions
├── packages/core/
│   └── src/
│       ├── scheduling/       # 空き時間・予算・配置・実行・小規模リスケ
│       ├── schemas/          # フォーム Zod
│       ├── ai/               # プロンプト・出力検証（目標分解・大規模リスケ・相談）
│       ├── mappers/
│       └── database-types.ts
├── supabase/
│   ├── migrations/
│   └── functions/
│       ├── goal-decompose/
│       ├── goal-approve-decompose/
│       ├── ai-settings/
│       ├── budget-calculate/
│       ├── schedule-generate/
│       ├── schedule-reschedule-minor/
│       ├── reschedule-major/
│       ├── ai-chat/
│       ├── export-data/
│       ├── delete-account/
│       └── _shared/          # auth, call-ai, ai-utils, schedule-utils, cors
└── docs/
```

---

## 6. Phase 1 で実装済みの機能

（変更なし。詳細は従来どおり）

- 認証、オンボーディング、基本設定、生活リズム、固定予定、ホーム（空き時間・当日変更）

---

## 7. Phase 2 で実装済みの機能

### 目標管理

- 目標 CRUD（`/goals`, `/goals/new`, `/goals/:id`）
- ステータス: `draft` → AI 分解承認後 `active`

### AI 目標分解（承認制）

1. **AIで分解する** — Edge Function `goal-decompose`（DB 未保存のプレビュー）
2. ユーザー確認
3. **この内容で作成** — Edge Function `goal-approve-decompose` → `goal_components` / `work_block_templates` 保存

**AI 呼び出し（`call-ai.ts`）**

- OpenAI / Gemini とも `maxOutputTokens: 8192`（`AI_LIMITS.goalDecomposeMaxOutputTokens`）
- Gemini の `MAX_TOKENS` または JSON パース失敗時、1 回自動リトライ
- プロンプトで出力サイズを抑制（summary 300 文字、components 12、workBlocks 24）

**Gemini で分解が失敗する場合**

- エラー「invalid JSON」「途中で切れ」→ 再試行ボタン、またはモデルを `gemini-2.0-flash` / OpenAI へ変更
- 429/403 → 無料枠上限。DB は変更されない
- **承認前に `estimatedTotalMinutes` 等を必ず目視確認**

### AI 設定（BYOK）

- `/settings/ai` — プロバイダ（OpenAI / Gemini）、モデル、API キー
- 接続テスト後 Vault 暗号化保存（`store_user_api_key` RPC）
- キーはクライアントに再表示しない（下4桁のみ）

### モデル選択肢（2026-05-24 時点）

| プロバイダ | 選択肢 | デフォルト |
| ---------- | ------ | ---------- |
| OpenAI | `gpt-4o-mini`, `gpt-4o`, `gpt-4.1-mini` | `gpt-4o-mini` |
| Gemini | `gemini-3.5-flash`, `gemini-2.0-flash`, `gemini-1.5-pro` | `gemini-3.5-flash` |

### レート制限

- 目標分解: **5 回/日**（`request_type = 'goal_decompose'`）
- 大規模リスケ: **3 回/日**（`request_type = 'reschedule'`）
- AI 相談: **20 回/日**（`request_type = 'chat'`、Phase 5）
- 月間トークン上限: `user_ai_settings.monthly_token_limit` 設定時に **enforcement あり**（Phase 5）。成功後 `tokens_used_this_month` に加算、`usage_reset_at` で月初リセット

---

## 8. Phase 3 で実装済みの機能

### 週次時間予算

- Edge Function `budget-calculate` — アクティブ目標から週あたり必要時間を算出し、今週の空き時間合計と比較して按分
- 不足時は `alerts` に警告 + 改善案を保存
- UI: `/budget` — 目標別の必要/割当/ステータス表示、「予算を再計算」ボタン

### 日次スケジュール生成（非 AI）

- Edge Function `schedule-generate` — 空き時間 + 作業ブロック + 週次予算をもとにスコアリング貪欲法で仮配置
- `schedules`（draft）+ `scheduled_blocks` として保存
- 固定予定衝突・起床就寝範囲外は `validation.ts` で拒否

### 承認フロー

- UI: `/schedule/approve` — 仮スケジュール確認、「再提案」「今日の予定に反映」
- 承認はクライアントから `schedules.status` を `draft` → `approved` に更新（RLS 保護）
- ホーム（`/`）— 承認済みブロック表示、draft がある場合は承認画面へ誘導

### 想定フロー

Phase 1〜4 通しのフローは §9 を参照。

```text
1. 目標を active にする（Phase 2）
2. /budget で「予算を再計算」
3. /schedule/approve で「今日の予定を生成」→ 内容確認 → 承認
4. ホームで承認済み予定を確認
```

### Phase 3 で意図的に簡略化した点

- カレンダーライブラリ（FullCalendar / schedule-x）は未導入。リスト UI のみ
- `user_choice` ブロックのメニュー選択 UI は未実装（タイトルのみ配置）

---

## 9. Phase 4 で実装済みの機能

### 完了記録

- UI: `/review` — 各作業ブロックを **完了 / 一部達成 / 未達成** で記録
- `scheduled_blocks.status`（`done` / `partial` / `skipped`）と `actual_minutes` を更新
- 進捗連動: `goals.completed_minutes`、今週の `goal_budgets.completed_minutes` に差分加算
- 全ブロック記録済み → `schedules.status` を `completed`、一部記録 → `in_progress`

### 振り返り

- 疲労度（1〜5）とメモを `schedules` に保存（`fatigue_level` / `review_note` / `reviewed_at`）
- **疲労度**は翌日の `schedule-generate` スコアリングに反映（前日の `fatigue_level` を参照）
  - 疲れ（4〜5）→ 軽い作業（`energy: low`）が選ばれやすい
  - 元気（1〜2）→ 負荷の高い作業が選ばれやすい
- **メモ**は DB 保存。`/review` 下部の **振り返り履歴**（直近 14 件）で再表示可能（Phase 5）。当日フォームへの再入力プリフィルは未実装

### 小規模リスケ（非 AI）

- Edge Function `schedule-reschedule-minor` — 未完了ブロックを翌日〜7日後の空き時間へ draft 再配置
- 元ブロックは `rescheduled` に更新
- UI: `/review` の「未完了を再配置する」→ 再配置先の `/schedule/approve?date=...` へ誘導

### 大規模リスケ（AI）

- Edge Function `reschedule-major` — 遅延目標を要約し、週次予算の調整案を JSON で返却（**プレビューのみ**）
- ユーザー承認後、クライアントから `goal_budgets.allocated_minutes` を更新 + `alerts` に提案を保存
- レート制限: **3 回/日**（`ai_request_logs.request_type = 'reschedule'`）
- BYOK 必須（`goal-decompose` と同様）

### 承認済み予定のキャンセル

- UI: `/schedule/approve` — 「キャンセル」で `schedules.status` → `cancelled`
- キャンセル後に「再提案」で draft 再生成可能（従来は承認済みがあると生成エラー）

### スコアリング実績連動

- `scoring.ts` — `ScoringExecutionContext`（疲労度・直近完了テンプレート ID）を配置スコアに反映
- `schedule-generate` 実行時、`_shared/schedule-utils.ts` が前日疲労度・直近14日の完了ブロックを取得

### 想定フロー（Phase 1〜4 通し）

```text
1. 目標を active にする（Phase 2）
2. /budget で「予算を再計算」（Phase 3）
3. /schedule/approve で「今日の予定を生成」→ 承認（Phase 3）
4. 実行後 /review で完了記録・振り返り（Phase 4）
5. 未完了があれば小規模リスケ → 再配置 draft を承認（Phase 4）
6. 大幅遅延時は大規模リスケ（AI）→ 週次予算を承認反映（Phase 4）
```

### Phase 4 で意図的に簡略化した点

- 大規模リスケは `goal_budgets.allocated_minutes` の更新のみ（GoalComponent 等は変更しない）
- `user_choice` ブロックのメニュー選択 UI は未実装

---

## 10. Phase 5 で実装済みの機能

### AI 相談

- UI: `/chat` — セッション内チャット（履歴はクライアント保持、DB 未保存）
- Edge Function `ai-chat` — 目標・週次予算・今日の予定・アラートをコンテキストに JSON 回答
- 口調: `user_ai_settings.ai_tone`（`polite` / `casual` / `concise`）を `/settings/ai` で設定
- レート制限: **20 回/日**、月間トークン上限（設定時）、BYOK 必須
- 出力検証: `chatOutputSchema` + **`normalizeChatOutput`**（`suggestedActions` の `payload` 等を除去）

### 月間トークン上限（enforcement）

- `_shared/ai-utils.ts` — `checkMonthlyTokenLimit` / `recordTokenUsage` / `assertAIUsageAllowed`
- 対象: `goal-decompose`、`reschedule-major`、`ai-chat`（呼び出し前チェック、成功後加算）
- 超過時: 429 + 「月間トークン上限に達しました」

### データ権（エクスポート・削除）

- UI: `/settings/data`
- `export-data`（GET）— 全ユーザーデータを JSON 返却（API キー本体は除外、下4桁のみ）
- `delete-account`（POST）— 確認文字列 `削除する` 必須 → Vault キー削除 → `auth.admin.deleteUser`（CASCADE）

### 通知プライバシー

- DB: `user_preferences.notification_settings` — `{ showDetailedAlerts, enablePushNotifications }`
- UI: `/settings/notifications`
- ホームのアラート: `showDetailedAlerts=false`（既定）時は伏せ字（`maskAlertMessage`）
- Web Push 本体は未実装（設定 UI のみ。`enablePushNotifications` は将来 PWA 用）

### AI 設定の拡張

- `/settings/ai` — 口調・月間上限を **API キーなし** でも更新可能（POST で `apiKey` 省略）
- 使用量表示: `tokensUsedThisMonth` / `monthlyTokenLimit`

### 振り返り履歴

- `/review` 下部 — `reviewed_at` があるスケジュールを直近 14 件表示（日付・疲労度・メモ）

### 想定フロー（Phase 1〜5 通し）

```text
1. 目標を active にする（Phase 2）
2. /budget で「予算を再計算」（Phase 3）
3. /schedule/approve で「今日の予定を生成」→ 承認（Phase 3）
4. 実行後 /review で完了記録・振り返り（Phase 4）
5. 未完了があれば小規模リスケ → 再配置 draft を承認（Phase 4）
6. 大幅遅延時は大規模リスケ（AI）→ 週次予算を承認反映（Phase 4）
7. 計画の相談は /chat（Phase 5）
8. データのバックアップ・退会は /settings/data（Phase 5）
```

### Phase 5 で意図的に簡略化した点

- カレンダー UI（月/週表示）は未導入
- `user_choice` ブロックのメニュー選択 UI は未実装
- Web Push 通知は設定項目のみ（送信ロジックなし）
- AI 相談履歴は DB 非保存（セッション内のみ）
- `purge_old_ai_request_logs` は RPC 定義のみ（定期実行 cron は未設定）

---

## 11. 画面ルート

| パス                    | 画面                         |
| ----------------------- | ---------------------------- |
| `/onboarding`           | 初回設定                     |
| `/`                     | ホーム（生活予定・作業予定・空き時間） |
| `/budget`               | 今週の時間予算               |
| `/schedule/approve`     | 仮スケジュール承認（`?date=` 対応・キャンセル可） |
| `/review`               | 進捗・振り返り（完了記録・リスケ・履歴） |
| `/chat`                 | AI 相談                      |
| `/goals`                | 目標一覧                     |
| `/goals/new`            | 目標登録                     |
| `/goals/:id`            | 目標詳細                     |
| `/goals/:id/decompose`  | AI 分解確認・承認            |
| `/settings/preferences` | 基本設定                     |
| `/settings/routines`    | 生活リズム                   |
| `/settings/fixed`       | 固定予定                     |
| `/settings/ai`          | AI プロバイダ・API キー・口調 |
| `/settings/notifications` | 通知プライバシー設定       |
| `/settings/data`        | データエクスポート・アカウント削除 |

---

## 12. データベース

### マイグレーション

| ファイル | 内容 |
| -------- | ---- |
| `20260522000000_phase1_schema.sql` | Phase 1 テーブル + RLS |
| `20260523000000_phase2_schema.sql` | 目標・AI 設定・ログ + Vault `store_user_api_key` |
| `20260524000000_vault_get_api_key.sql` | Edge Functions 用 `get_api_key_by_ref` RPC |
| `20260525000000_phase3_schema.sql` | 時間予算・スケジュール・アラート + RLS |
| `20260526000000_phase4_schema.sql` | 振り返り列（`schedules`）+ ブロック status インデックス |
| `20260527000000_phase5_schema.sql` | 通知設定（`notification_settings`）、AI 口調（`ai_tone`）、ログ purge RPC |

Phase 1 詳細: [`テーブル設計_Phase1.md`](./テーブル設計_Phase1.md)

### Phase 2 テーブル

| テーブル | 用途 |
| -------- | ---- |
| `goals` | 長期目標 |
| `goal_components` | 目標構成要素 |
| `work_block_templates` | 作業ブロックテンプレート |
| `user_ai_settings` | プロバイダ・モデル・Vault 参照・月間上限・口調（`ai_tone`） |
| `ai_request_logs` | AI 呼び出しログ（マスキング済み要約） |

### Phase 3 テーブル

| テーブル | 用途 |
| -------- | ---- |
| `goal_budgets` | 週次時間予算（required / allocated / status） |
| `schedules` | 日次スケジュール（draft / approved 等）+ 振り返り（`fatigue_level`, `review_note`, `reviewed_at`） |
| `scheduled_blocks` | 配置された作業ブロック + 実績（`status`, `actual_minutes`） |
| `alerts` | 達成困難・時間不足の警告 |

### Phase 5 スキーマ追加

| 対象 | 内容 |
| ---- | ---- |
| `user_preferences.notification_settings` | JSONB。`showDetailedAlerts`（既定 false）、`enablePushNotifications` |
| `user_ai_settings.ai_tone` | `polite` / `casual` / `concise` |
| `purge_old_ai_request_logs()` | service_role のみ。90 日超の `ai_request_logs` 削除 |

---

## 13. コアロジック（`packages/core`）

### 空き時間計算（Phase 1）

`packages/core/src/scheduling/free-time.ts` — `calculateFreeTime` ほか

### 時間予算・日次配置（Phase 3）

```text
packages/core/src/scheduling/
├── budget.ts             # 週次必要時間・複数目標按分
├── scoring.ts            # 配置候補スコアリング
├── placement.ts          # 貪欲法による仮配置
├── validation.ts         # 固定予定衝突・範囲チェック
├── execution.ts          # 完了記録・進捗加算・再配置判定
├── reschedule-minor.ts   # 小規模リスケ（翌日以降への再配置）
├── weekly-free-time.ts   # 週合計空き時間
└── week-utils.ts         # 月曜始まりの週期間（date-fns 非依存）
```

Phase 4 追加分: `execution.ts`, `reschedule-minor.ts`、`ai/prompts|schemas/reschedule.ts`。`scoring.ts` は疲労度・過去選択を `ScoringExecutionContext` で受け取る。

**Edge Function 向け注意**: `week-utils.ts` は Deno 互換のため **date-fns を使わない**。`packages/core` に npm 依存を増やす場合は Edge Function バンドル可否を確認すること。

### AI 目標分解・大規模リスケ・相談（Phase 2 / 4 / 5）

```text
packages/core/src/ai/
├── constants.ts
├── prompts/goal-decompose.ts
├── prompts/reschedule.ts       # Phase 4: 大規模リスケ
├── prompts/chat.ts             # Phase 5: AI 相談
├── schemas/goal-decompose.ts
├── schemas/reschedule.ts
├── schemas/chat.ts             # Phase 5: chatOutputSchema
├── normalize-goal-decompose.ts
└── normalize-chat.ts           # Phase 5: suggestedActions 正規化
```

Phase 5 追加分: `schemas/notification-settings.ts`（`maskAlertMessage` 等）

### テスト

```bash
pnpm --filter @ai-scheduler/core test   # 32 tests
```

---

## 14. Edge Functions

| 関数 | 役割 |
| ---- | ---- |
| `goal-decompose` | 目標を AI 分解 → 正規化 → Zod 検証 → プレビュー返却 |
| `goal-approve-decompose` | 承認内容を再検証 → DB 保存 → `active` |
| `ai-settings` | GET: 設定状態 / POST: 接続テスト + Vault 保存 |
| `budget-calculate` | 週次時間予算計算 → `goal_budgets` upsert + アラート |
| `schedule-generate` | 日次仮スケジュール生成 → `schedules` / `scheduled_blocks` 保存 |
| `schedule-reschedule-minor` | 未完了ブロックの小規模リスケ（非 AI）→ draft 再配置 |
| `reschedule-major` | 大規模リスケ AI プレビュー（週次予算調整案） |
| `ai-chat` | AI 相談（目標・予算・予定コンテキスト付き） |
| `export-data` | ユーザーデータ JSON エクスポート |
| `delete-account` | アカウント・Vault キー・全データ削除 |

**デプロイ前に必須:**

```bash
pnpm --filter @ai-scheduler/core build   # dist を最新化
supabase db push                         # 未適用マイグレーションがある場合
supabase functions deploy goal-decompose
supabase functions deploy goal-approve-decompose
supabase functions deploy ai-settings
supabase functions deploy budget-calculate
supabase functions deploy schedule-generate
supabase functions deploy schedule-reschedule-minor
supabase functions deploy reschedule-major
supabase functions deploy ai-chat
supabase functions deploy export-data
supabase functions deploy delete-account
```

**import ルール（重要）**

| 関数種別 | dist の import 方法 | deno.json |
| -------- | ------------------- | --------- |
| AI 系（`goal-decompose`, `reschedule-major`, `ai-chat` 等） | `dist/ai/*`, `dist/schemas/*` 等（`index.js` 可） | **必須**（`zod` → `npm:zod@3.25.67`） |
| 非 AI（`budget-calculate`, `schedule-generate`, `schedule-reschedule-minor`） | **`dist/index.js` 禁止**。`dist/scheduling/*.js`, `dist/mappers/*.js` を直接 import | 不要 |

`_shared/schedule-utils.ts` は上記非 AI ルールに従う。`dist/index.js` は zod 付き schemas / ai を再 export するため、Deno が `zod` 解決エラーになる。

---

## 15. フロントエンドの設計方針

| 関心 | 置き場所 |
| ---- | -------- |
| 生活リズム等 CRUD | `hooks/useScheduleData.ts` |
| 目標・AI 設定 | `hooks/useGoals.ts` |
| 時間予算・アラート | `hooks/useBudgets.ts` |
| 日次スケジュール | `hooks/useSchedules.ts` |
| 完了記録・振り返り・リスケ | `hooks/useExecution.ts` |
| AI 相談 | `hooks/useChat.ts` |
| 通知設定・振り返り履歴 | `hooks/usePhase5.ts` |
| データエクスポート・削除 | `hooks/useDataRights.ts` |
| Edge Function 呼び出し | `lib/edge-functions.ts` |
| Query Key | `lib/query-keys.ts` |

---

## 16. 環境構築・デプロイ（要約）

```bash
pnpm install
supabase login
supabase link --project-ref <project-ref>   # 必須。未 link だと db push 失敗
supabase db push

# Phase 1 済み DB で push が失敗する場合:
supabase migration repair 20260522000000 --status applied
supabase db push

cp apps/web/.env.example apps/web/.env
pnpm --filter @ai-scheduler/core build
pnpm dev
```

Edge Functions 利用時は §14 の deploy も実行。

---

## 17. 既知の技術的判断・注意点

### Phase 1 由来（変更なし）

- `database-types.ts` は手動管理（`supabase gen types` 出力を反映）
- 「ごろ」は対称 ±30 分
- 当日変更 skip は空き時間計算未反映
- `target_date` はブラウザローカル日付（SQL Editor の UTC とずれる）
- 固定予定 `commute_minutes` は終了時刻の後のみ

### Phase 2 追加

- AI 分解の正規化デフォルト — **承認前に内容を必ず目視確認**
- Gemini 無料枠上限 — 429/403 で分解失敗
- Gemini 出力途切れ — 大きな目標で JSON 不完全になることがある（`maxOutputTokens` + リトライで緩和。それでも失敗時はモデル変更を検討）

### Phase 3 追加

- 週次空き時間計算は当日変更を含めない（計画用）
- 日次生成は当日変更を反映
- README は Phase 1 記載のまま（本ドキュメントを参照）

### Phase 4 追加

- 当日の振り返りフォームは再訪時にメモをプリフィルしない（履歴セクションで過去分は閲覧可）
- 小規模リスケは 1 ブロックにつき最初に見つかった空き日へ 1 件配置（同一日に複数まとめない）
- 大規模リスケ承認は `goal_budgets.allocated_minutes` の上書きのみ
- 大規模リスケ: **3 回/日**（目標分解は 5 回/日）

### Phase 5 追加

- AI 相談: Gemini が `suggestedActions` に `payload` 等を付けることがある → **`normalizeChatOutput` + `ai-chat` 再 deploy** で対応
- 月間トークン上限未設定時は enforcement なし（加算のみ）
- `delete-account` は `auth.admin.deleteUser` 使用（service_role 必須）
- 通知の Web Push は UI 準備のみ

### Edge Function デプロイ

- **`Failed to bundle … date-fns`** → core の scheduling モジュールに npm 依存を入れない（または Deno 用 import map が必要）
- **`Failed to bundle … zod`** → 非 AI 関数で `dist/index.js` を import していないか確認
- AI 関数を変更したら **`goal-decompose` / `reschedule-major` / `ai-chat` を再 deploy**（`call-ai.ts` / `ai-utils.ts` は `_shared` 経由で共有）

---

## 18. 任意拡張（未着手）

- [ ] カレンダー UI（`@schedule-x/react` vs FullCalendar の PoC）
- [ ] `user_choice` ブロックのメニュー選択 UI
- [ ] Web Push 通知の実装（`enablePushNotifications` 連動）
- [ ] AI 相談履歴の DB 永続化
- [ ] 振り返りフォームへの当日メモ再入力（プリフィル）
- [ ] `purge_old_ai_request_logs` の定期実行（pg_cron 等）
- [ ] README の Phase 5 反映

---

## 19. 新チャットへの指示例

```
AGENTS.md と docs/開発作業_引き継ぎ.md を読んでから作業を開始してください。
Phase 0〜5 は実装済みです。§18 の任意拡張、または不具合修正から着手してください。
```

---

## 20. 関連ドキュメント一覧

| ファイル | 内容 |
| -------- | ---- |
| [`AGENTS.md`](../AGENTS.md) | コーディング規約 |
| [`README.md`](../README.md) | 環境構築（Phase 1 記載、要更新） |
| [`技術選定と実装方針.md`](./技術選定と実装方針.md) | フェーズ計画 |
| [`テーブル設計_Phase1.md`](./テーブル設計_Phase1.md) | Phase 1 DB |
| [`AI秘書スケジュール管理アプリ_詳細企画書_セキュリティ反映版.md`](./AI秘書スケジュール管理アプリ_詳細企画書_セキュリティ反映版.md) | 機能・セキュリティ要件 |
| 本ファイル | 作業引き継ぎサマリー |
